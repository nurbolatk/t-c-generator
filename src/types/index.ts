export type TermsFormValues = {
  name: string;
  url: string;
  address: string;
  city: string;
  zip: string;
  state: string;
  country: string;
  email: string;
};

export type UserData = {
  id: number;
  name: string;
  url: string;
  email: string;
};
